long long findMinSum(vector<long long>& A, vector<long long>& B) {
    sort(A.begin(), A.end());
    sort(B.begin(), B.end());

    long long sum = 0;
    for (int i = 0; i < A.size(); ++i) {
        sum += llabs(A[i] - B[i]);
    }
    return sum;
}

